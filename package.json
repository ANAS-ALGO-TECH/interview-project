{
  "name": "realtime-collaboration-dashboard",
  "version": "1.0.0",
  "description": "A real-time task management board with WebSocket support",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "setup": "npm run install-all",
    "start": "npm run dev"
  },
  "keywords": ["react", "nodejs", "websocket", "mongodb", "task-management"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
